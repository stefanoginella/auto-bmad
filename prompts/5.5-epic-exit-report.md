/bmad-analyst yolo - Generate an Epic Exit Report for epic {{EPIC_ID}}.

Read all story files for this epic at these paths:
{{EPIC_STORY_PATHS}}

For each story, read the "Human Review Required" section (and fall back to "Auto-bmad Completion" if "Human Review Required" doesn't exist).

Produce a single Epic Exit Report file at {{EPIC_EXIT_REPORT}} with these sections:

## Epic Exit Report — Epic {{EPIC_ID}}

### Carry-Forward Items
All deferred items accumulated across the epic. Source: the LAST story's "Deferred to Future Stories" section (which is a rolling accumulator containing all unresolved defers). List each with: finding ID, title, description, and which story originally surfaced it.
If none: "No carry-forward items."

### Unresolved Open Questions
All items marked with a red circle Open across all stories, organized by story ID. For each: the question, context, and why it couldn't be resolved.
If none: "No unresolved questions."

### Unchecked Manual Testing
All unchecked (`- [ ]`) manual testing items across all stories, organized by story ID. For each: the test item and relevant code location.
If none: "All manual testing items have been addressed."

### Upstream Issues Identified
All [bad_spec] Change Log entries that reference upstream artifacts (architecture, PRD, epic). These documents may need revision before the next epic. For each: finding ID, which upstream artifact, what was wrong, and what was decided.
If none: "No upstream issues identified."

### Decisions to Validate
All [bad_spec] and [intent_gap] resolutions the AI made across the entire epic that a human should confirm. Organized by story ID. For each: finding ID, what was decided, and the rationale.
If none: "No decisions require validation."
