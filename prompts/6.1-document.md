/bmad-agent-tech-writer yolo - Finalize documentation for story {{STORY_ID}}. The story file is at: {{STORY_REF}}

{{GIT_CONTEXT}}

Perform ALL of the following:

1. **Verify story file completeness.** Read the story file and check:
   - Status should be 'review' or later (not 'in-progress' or 'ready-for-dev')
   - All Tasks/Subtasks should be checked [x] (flag any that are unchecked)
   - Dev Agent Record: Agent Model Used, Completion Notes List, and Debug Log References should all be populated
   - Change Log must include entries for spec triage fixes (Phase 1, if those steps ran). For code triage patches ([patch:post-impl] from Phase 3), a summary line is sufficient rather than per-finding detail — e.g., "N code triage patches applied (M critical). See [code-triage]({{STORY_ARTIFACTS}}/{{STORY_SHORT_ID}}-3.3-code-triage.md)."
   Fix anything that is missing or incomplete.

2. **Populate pipeline report.** Read the pipeline report at {{PIPELINE_REPORT}} and:

   a. Populate the 'Reviewer Assessment' section:
      - Use the 'Reviewer Anonymization Mapping' sections to deanonymize reviewer labels back to actual provider names
      - Read reviewer signal assessment from both triage reports (spec triage 1.3 and code triage 3.3, if they exist)
      - Fill in the per-phase table (Model | Phase | Findings | Accepted | Signal | Notes)
      - Fill in the aggregate table (Model | Total Findings | Accepted | Accept Rate | Avg Signal)
      - Remove the HTML comment placeholder and the anonymization mapping sections

   b. Populate the 'Suggested Review Order' section:
      - Read all code changes (commits {{COMMIT_BASELINE}}..HEAD) and triage reports
      - Organize stops by CONCERN (e.g., 'state management', 'error handling'), not by file
      - Each stop: file path with line number, one-line framing of what to look for
      - Order so each stop builds on context from the previous one
      - Prioritize stops around areas where triage findings concentrated
      - Remove the HTML comment placeholder

   c. Populate the 'File List' section with ALL files created, modified, or deleted across the entire pipeline (not just implementation — check git diff --stat for any files triage fixes touched). Organize by category: new, modified, deleted, acceptance tests, pipeline artifacts.

3. **Add Human Review Required section** to the story file, BEFORE the Auto-bmad Completion section. Four subsections:

   ### Decisions to Validate
   All [bad_spec] and [intent_gap] Change Log entries from this story. For each: what was decided, which upstream artifact (architecture, PRD, epic) informed the decision, and the rationale. Flag any that reference upstream documents as potentially needing manual upstream revision.
   For entries also tagged [ai-resolved]: note "(AI-validated)" after the finding and include the confidence level from the resolution report at {{STORY_ARTIFACTS}}/{{STORY_SHORT_ID}}-6.0-ai-resolution.md. The human reviewer can prioritize non-validated items.
   If none: "No spec decisions were made that require validation."

   ### Manual Testing Required
   A checklist (checkboxes `- [ ]`) of items requiring manual verification. For each item: reference the specific acceptance criterion and relevant code location (file:line).
   For items marked [ai-tested] in the resolution report ({{STORY_ARTIFACTS}}/{{STORY_SHORT_ID}}-6.0-ai-resolution.md): mark as `- [x]` with note "(covered by automated test — see resolution report)".
   If none: "No manual testing required."

   ### Open Questions
   Unresolved items from the pipeline. Read the spec triage report ({{STORY_ARTIFACTS}}/{{STORY_SHORT_ID}}-1.3-spec-triage.md) and code triage report ({{STORY_ARTIFACTS}}/{{STORY_SHORT_ID}}-3.3-code-triage.md) — if either contains a '## Unresolvable Findings' section, include each finding here. Mark each with 🔴 Open or 🟡 Needs Input. Include: finding ID, title, what is missing, why it could not be resolved, and the originating triage phase (spec or code).
   For items promoted to 🔴🔴 Critical by step 6.0: include with double red circle marker and "(AI-escalated)" after the title. These take priority over 🔴 Open items.
   If none: "No open questions."

   ### Deferred to Future Stories
   All [defer] and [defer:post-impl] Change Log entries from THIS story only. For each: finding ID, title, one-line description, and which phase surfaced it.
   If none: "No deferred items."

   If ALL four subsections are empty, replace the entire section with: "## Human Review Required\n\nNo human action required."

4. **Add Auto-bmad Completion section** with ONLY:

   ### Commit Message
   The commit message for all changes in story {{STORY_ID}} inside a code block, following Conventional Commits 1.0.0 (<type>(<scope>): <subject> headline with <summary> body after a blank line). Type: build|chore|ci|docs|feat|fix|perf|refactor|revert|style|test.

   ### Pipeline Report
   One-line summary (steps passed/failed, wall time, files changed) with link to [pipeline-report.md]({{STORY_ARTIFACTS}}/{{STORY_SHORT_ID}}-6.1-pipeline-report.md).

   ### Artifacts
   Table linking to each pipeline artifact with a one-line outcome (pass/fail/N findings). Skip any that don't exist:
   | Artifact | Path | Outcome |
   - Pipeline report: {{STORY_ARTIFACTS}}/{{STORY_SHORT_ID}}-6.1-pipeline-report.md
   - Spec triage: {{STORY_ARTIFACTS}}/{{STORY_SHORT_ID}}-1.3-spec-triage.md
   - Code acceptance audit: {{STORY_ARTIFACTS}}/{{STORY_SHORT_ID}}-3.2-code-acceptance.md
   - Code triage: {{STORY_ARTIFACTS}}/{{STORY_SHORT_ID}}-3.3-code-triage.md
   - AI resolution report: {{STORY_ARTIFACTS}}/{{STORY_SHORT_ID}}-6.0-ai-resolution.md

   Do NOT include Pipeline Summary, Review Summary, Notable Decisions, Pipeline Learnings, or File List — these belong in the pipeline report, not the story file.
