/bmad-agent-tech-writer yolo - Finalize documentation for story {{STORY_ID}}. The story file is at: {{STORY_REF}}

{{GIT_CONTEXT}}

Perform ALL of the following:

1. Read the story file and verify all sections that previous pipeline steps should have populated are present and up to date:
   - Status should be 'review' or later (not 'in-progress' or 'ready-for-dev')
   - All Tasks/Subtasks should be checked [x] (flag any that are unchecked)
   - Dev Agent Record: Agent Model Used, Completion Notes List, and Debug Log References should all be populated
   - File List must reflect ALL files created, modified, or deleted across the entire pipeline (not just implementation — check git status for any files the triage fixes touched that are not listed)
   - Change Log must include entries for spec triage patch fixes (Phase 1, if those steps ran) and code triage patch fixes (Phase 3, if those steps ran)
   Fix anything that is missing or incomplete.

2. Add an '## Auto-bmad Pipeline Artifacts' section after the Dev Agent Record. This is a reference index ONLY — do not duplicate content already in the Dev Agent Record, Change Log, or the artifacts themselves. List each artifact with a one-line outcome (pass/fail/N findings). Skip any that don't exist:
   - Pipeline report: {{STORY_ARTIFACTS}}/{{STORY_SHORT_ID}}-6.1-pipeline-report.md
   - Spec triage: {{STORY_ARTIFACTS}}/{{STORY_SHORT_ID}}-1.3-spec-triage.md
   - Code acceptance audit: {{STORY_ARTIFACTS}}/{{STORY_SHORT_ID}}-3.2-code-acceptance.md
   - Code triage: {{STORY_ARTIFACTS}}/{{STORY_SHORT_ID}}-3.3-code-triage.md

3. Read the pipeline report at {{PIPELINE_REPORT}} and populate the 'Reviewer Assessment' section:
   - The pipeline report contains 'Reviewer Anonymization Mapping' sections that map anonymous labels (Reviewer A, B, C) back to actual review sessions. Use these mappings to deanonymize the reviewer signal assessment tables from triage reports — the final pipeline report should show actual provider names, not anonymous labels.
   - Read the reviewer signal assessment section from both triage reports (spec triage 1.3 and code triage 3.3, if they exist)
   - Fill in the per-phase table (Model | Phase | Findings | Accepted | Signal | Notes)
   - Fill in the aggregate table (Model | Total Findings | Accepted | Accept Rate | Avg Signal)
   - Remove the HTML comment placeholder and the anonymization mapping sections (they've served their purpose)

4. Populate the 'Suggested Review Order' section in the pipeline report. This is a navigable review trail for human reviewers:
   - Read all code changes (commits {{COMMIT_BASELINE}}..HEAD) and the triage reports to understand what changed and where findings concentrated
   - Organize stops by CONCERN (e.g., 'state management', 'error handling', 'API contract'), not by file
   - Each stop: file path with line number, one-line framing of what to look for at that stop
   - Order stops so each builds on context from the previous one (data model before API, API before UI)
   - Prioritize stops around areas where triage findings were concentrated
   - Remove the HTML comment placeholder

5. Add a '## Human Review Required' section BEFORE the Auto-bmad Completion section. This consolidates all items requiring human attention from this story. It has four subsections:

   ### Decisions to Validate
   All [bad_spec] and [intent_gap] Change Log entries from this story. For each: what was decided, which upstream artifact (architecture, PRD, epic) informed the decision, and the rationale. Flag any that reference upstream documents as potentially needing manual upstream revision.
   If none: "No spec decisions were made that require validation."

   ### Manual Testing Required
   A checklist (checkboxes `- [ ]`) of items requiring manual verification. For each item: reference the specific acceptance criterion and relevant code location (file:line).
   If none: "No manual testing required."

   ### Open Questions
   Unresolved items from the pipeline. Mark each with 🔴 Open or 🟡 Needs Input. Include context on why it couldn't be resolved automatically.
   If none: "No open questions."

   ### Deferred to Future Stories
   All [defer] Change Log entries from THIS story. For each: finding ID, title, one-line description, and which phase surfaced it.{{PREV_STORY_DEFERRED}}
   If none (including no carry-forward): "No deferred items."

   If ALL four subsections are empty, replace the entire section with: "## Human Review Required\n\nNo human action required."

6. Add an '## Auto-bmad Completion' section with ONLY information not already captured in the Dev Agent Record or Change Log:

   ### Pipeline Summary
   - Duration, models used, result (steps passed, files changed), link to pipeline-report.md

   ### Review Summary
   A summary table:
   | Phase | Step | Findings | Patch | Defer | Intent Gap | Bad Spec | Report |
   With one row per review artifact (spec triage, acceptance audit, code triage) linking to the file.

   ### Notable Decisions
   Key decisions made during the pipeline (not already in Change Log).

   ### Pipeline Learnings
   Useful observations from the pipeline run.

   ### Commit Message
   The commit message for all changes in story {{STORY_ID}} inside a code block, following Conventional Commits 1.0.0 (<type>(<scope>): <subject> headline with <summary> body after a blank line). Type: build|chore|ci|docs|feat|fix|perf|refactor|revert|style|test.

   Do NOT repeat completion notes, file lists, decision logs, or fix summaries — those are already in the Dev Agent Record and Change Log.